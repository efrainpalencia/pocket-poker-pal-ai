{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Uvicorn (cwd=src)",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "cwd": "${workspaceFolder}/src",
      "args": ["main:app", "--reload", "--port", "8000"],
      "console": "integratedTerminal"
    }
  ]
}
